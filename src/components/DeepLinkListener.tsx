"use client";


// import { useElectron } from "@/hooks/use-electron";

export function DeepLinkListener() {
    // Web apps handle deep links via standard routing.
    // Use Next.js hooks if you need to catch specific query parameters on mount.
    // For now, this component is a no-op in the Web version.
    return null;
}
